'use client'

/** Placeholder — Fase 3.4 vai trazer toggle "Comunicação Automática",
 * brand_display_name/tone, send_window, default_channel_priority etc.
 * Endpoints: GET/PATCH /communication/settings. */
export default function ConfiguracoesTab() {
  return (
    <div
      className="p-6 rounded-xl text-center"
      style={{ background: '#0c0c10', border: '1px solid #1a1a1f' }}
    >
      <p className="text-zinc-500 text-sm">
        🚧 Aba em construção — Fase 3.4
      </p>
    </div>
  )
}
