'use client'

/** Placeholder — Fase 3.3 vai trazer editor de templates com preview
 * WhatsApp em tempo real. Endpoints: GET/POST/PATCH/DELETE
 * /communication/templates. Soft delete (is_active=false). */
export default function TemplatesTab() {
  return (
    <div
      className="p-6 rounded-xl text-center"
      style={{ background: '#0c0c10', border: '1px solid #1a1a1f' }}
    >
      <p className="text-zinc-500 text-sm">
        🚧 Aba em construção — Fase 3.3
      </p>
    </div>
  )
}
